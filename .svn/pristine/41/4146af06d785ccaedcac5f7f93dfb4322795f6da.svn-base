#include <iostream>
#include "Triangulation.h"
#include "Map.h"
#include "Pathfinder.h"
#include "NavGraph.h"

using namespace std;


int main() {
	/*Pathfinder buscar;
	NavGraph ng;
	int ids[5];
	ids[0]=ng.addNode(Point(1,1));
	ids[1]=ng.addNode(Point(1,3));
	ids[2]=ng.addNode(Point(4,1));
	ids[3]=ng.addNode(Point(2,2));
	ids[4]=ng.addNode(Point(4,4));

	ng.addEdge(ids[0],ids[1]);
	ng.addEdge(ids[0],ids[2]);
	ng.addEdge(ids[0],ids[3]);

	ng.addEdge(ids[3],ids[1]);
	ng.addEdge(ids[3],ids[2]);

	ng.addEdge(ids[2],ids[4]);
	ng.addEdge(ids[1],ids[4]);

	//cout << ng;
	ASNode origen = ng.getNode(ids[0]);
	ASNode destino = ng.getNode(ids[4]);
/*
	buscar.navigationGraph = ng;
	//cout << buscar.navegacion;
	//cout << origen << destino;
	vector<ASNode> caminoGuay = buscar.AStar(origen.getPosition(),destino.getPosition());

	for (unsigned int i = 0; i < caminoGuay.size(); i++){
		cout << caminoGuay[i].getId() << " | " << caminoGuay[i].getPosition() << endl;
	}
*/
	return 0;
}
